# Uno MVVMExpress for Visual Studio

Tools → **Uno MVVMExpress** → Create New App… / Add Page…

Installs `Plugin.Uno.MVVMExpress.Templates` and runs `dotnet new`. Marketplace upload is manual from the `ide-extensions` workflow artifact.
